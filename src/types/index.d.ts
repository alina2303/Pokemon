// eslint-disable-next-line no-unused-vars
interface PokemonName {
    name: string;
    url: string;
}

// eslint-disable-next-line no-unused-vars
interface PokemonSimple {
    id: number;
    name: string;
    height: number;
    weight: number;
    abilities: string[];
    image: string;
}

// eslint-disable-next-line no-unused-vars
interface PokemonAbilities {
    id: number;
    name: string;
}

// eslint-disable-next-line no-unused-vars
interface PokemonAbilitiesPokemon{
    pokemonId: number;
    abilityId: number;
}

